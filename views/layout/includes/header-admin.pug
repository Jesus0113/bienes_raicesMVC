header.bg-slate-700
    .container.mx-auto.flex.justify-between.items-center
        a(href="/") 
            h1(class=" text-4xl my-10 font-extrabold text-center text-slate-300 ") Inmo
                span.font-normal.text-slate-400 biliaria

        nav(class=" my-7 text-sm md:flex md:items-center md:gap-3 font-bold text-white ") 
            a(href="/mis-propiedades") Mis Propiedades
            a(href="") Mi Perfil

            form(action="/auth/cerrar-sesion" method="POST")
                input(type="hidden" name="_csrf" value=csrfToken ) 
                input(type="submit" class="bg-slate-400 hover:bg-slate-500  px-5 py-3 rounded-lg cursor-pointer" value="Cerrar Sesion")